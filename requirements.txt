# ==================== Browser Dome Dependencies ====================
# Core Browser Dome automation (Playwright already listed below)
mitmproxy>=11.1.2                    # Transparent traffic interception + HAR export
playwright-stealth>=1.0.0           # Headless browser detection evasion
Pillow>=10.0.0                      # Screenshot annotation (SoM overlays, bounding boxes)
opencv-python>=4.8.0                # Image processing for SoM annotation
# HAR/artifact packaging
haralyzer>=1.5.3                    # HAR file parsing and validation

# Core dependencies
rich==14.1.0
pydantic>=2.0.0
python-dotenv>=1.0.0
PyYAML>=6.0

# AI/ML dependencies
--extra-index-url https://download.pytorch.org/whl/cu124
torch>=2.4.0
transformers>=4.36.0
qwen-omni-utils

# ASR (Speech-to-Text) - Whisper for SpeechRecognizer
openai-whisper>=0.1.0
llama-cpp-python>=0.3.16
llama-models>=0.3.0
gguf==0.18.0
accelerate>=0.24.0
safetensors>=0.4.0
bitsandbytes>=0.41.0
peft>=0.7.0
sentence-transformers>=2.2.2

# Multimodal dependencies
pillow>=10.0.0
opencv-python>=4.8.0
librosa>=0.10.0
soundfile>=0.12.0

# TTS (Text-to-Speech) - Fallback chain: CosyVoice > Coqui > pyttsx3
# pyttsx3 is the final offline fallback (no GPU required)
pyttsx3>=2.90

# API dependencies
fastapi>=0.104.0
uvicorn>=0.24.0
websockets>=12.0
python-multipart>=0.0.6
PySocks>=1.7.1

# Database dependencies
sqlalchemy>=2.0.0

# Async dependencies
aiohttp>=3.9.0

# Monitoring/Logging
prometheus-client>=0.19.0
python-json-logger>=2.0.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0

# Security
cryptography>=41.0.0
pycryptodome>=3.19.0
PyJWT>=2.8.0

# Utilities
numpy>=1.24.0
pandas>=2.0.0
requests>=2.31.0
psutil>=5.9.0
click>=8.0.0
dnspython>=2.4.0
PyQt6==6.10.1
PyQt6-WebEngine==6.10.0

# Vision/Desktop Control dependencies
pyautogui>=0.9.53
mss>=7.0.1
pytesseract>=0.3.10
pexpect>=4.9.0

# Learning/RAG dependencies
chromadb>=0.4.0
PyPDF2>=3.0.0

# Pentesting AI assistant
nebula-ai
sounddevice==0.5.3
colorama>=0.4.6

# Added from requirements_production.txt (no version modified)
nvidia-ml-py==13.595.45
httpx>=0.26.0
tqdm>=4.66.0


# datasets
datasets>=4.4.2

playwright==1.57.0

# TTS fallback engines (CosyVoice 3.0 is primary, these are fallbacks)
coqui-tts==0.27.3  # Secondary TTS fallback (GPU-accelerated)

# ==================== RF/SDR Dependencies ====================
# RTL-SDR Python wrapper
pyrtlsdr>=0.3.0

# Signal processing
scipy>=1.11.0
pyfftw>=0.13.0
numba>=0.58.0

# Audio streaming for demodulation
pyaudio>=0.2.14

# Optional: Hardware abstraction (install based on device)
# SoapySDR Python bindings (for multi-device support)
# pysoapy>=0.1.0  # Uncomment if using SoapySDR

# Optional: USRP support
# pyuhd>=4.1.0  # Uncomment if using USRP

# Optional: BladeRF support
# pybladeRF>=2.0.0  # Uncomment if using BladeRF

# Note: System packages required for full SDR functionality:
# Ubuntu/Debian: sudo apt install -y rtl-sdr gr-gsm gr-lte urh inspectrum gnuradio hackrf bladerf uhd-host soapy-sdr
# These are NOT pip packages - install via system package manager

# ==================== Crawl4AI Dependencies ====================
# LLM-ready web crawling integration (April 2026)
unclecode-litellm>=1.81.13            # Security-patched litellm for LLM extraction
rank-bm25~=0.2                         # BM25 relevance ranking for adaptive crawling

# ==================== Scrapling Integration Dependencies ====================
# TLS fingerprint impersonation for stealthy HTTP fetching (April 2026)
curl_cffi>=0.5.0                       # TLS fingerprint impersonation, HTTP/2, HTTP/3
cssselect>=1.2.0                       # CSS selector parsing for adaptive scraping
orjson>=3.9.0                          # Fast JSON serialization for fingerprint storage
lxml>=5.0.0                            # HTML parsing and XPath support

# ==================== The Well Physics Simulation Dependencies ====================
# Neural Operator & PDE Surrogate Modeling (April 2026)
h5py>=3.12.0                           # HDF5 data loading for physics datasets
einops>=0.8.0                          # Tensor operations for neural operators
wandb>=0.18.0                          # Experiment tracking (optional, for training)
tensorly>=0.8.0                        # Tensor decomposition for LITEFNO
